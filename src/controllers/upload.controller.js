const uploadFile = async (req, res) => {
  //uploadFile api logic here the life should pe store in public/uploads folder
};

const uploadController = {
  uploadFile,
};
module.exports = uploadController;
